{ colors }:

with colors; ''
set default-bg                  "#${base06}"
set default-fg                  "#${base01}"

set statusbar-fg                "#${base02}"
set statusbar-bg                "#${base04}"

set inputbar-bg                 "#${base00}"
set inputbar-fg                 "#${base02}"

set notification-error-bg       "#${base08}"
set notification-error-fg       "#${base06}"

set notification-warning-bg     "#${base08}"
set notification-warning-fg     "#${base06}"

set highlight-color             "#${base0A}"
set highlight-active-color      "#${base08}"

set completion-highlight-fg     "#${base05}"
set completion-highlight-bg     "#${base0C}"

set completion-bg               "#${base05}"
set completion-fg               "#${base0C}"

set notification-bg             "#${base0B}"
set notification-fg             "#${base07}"

set recolor-lightcolor          "#${base06}"
set recolor-darkcolor           "#${base02}"
set recolor                     "false"

# setting recolor-keephue true will keep any color your pdf
# has.  If it is set to false, it will just be shades of
# gray.  (lol 50 shades of gray)
set recolor-keephue             "true"

# Rest of your configs go here:
''
